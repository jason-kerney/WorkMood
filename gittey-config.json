{
    "branchPrefix": {
        "separator": "",
        "validator": "",
        "prefixes": {}
    },
    "commitPrefix": {
        "separator": " ",
        "validator": "^.{1,100}$",
        "prefixes": {}
    },
    "verboseMode": false,
    "aliases": [
        {
            "name": "push",
            "command": "echo 'Please use platform-specific push commands: gittey:push:windows or gittey:push:mac'"
        },
        {
            "name": "push:windows",
            "command": "gittey docs && gittey check:windows && gittey commit && git push",
            "description": "Run tests, commit changes, and push to remote (Windows)"
        },
        {
            "name": "push:mac",
            "command": "gittey docs && gittey check:mac && gittey commit && git push",
            "description": "Run tests, commit changes, and push to remote (Mac)"
        },
        {
            "name": "check:windows",
            "command": "cls && powershell -ExecutionPolicy Bypass -File generate-coverage-report.ps1",
            "description": "Run tests and generate coverage report (Windows)"
        },
        {
            "name": "check:mac",
            "command": "clear && chmod +x generate-coverage-report.sh && ./generate-coverage-report.sh",
            "description": "Run tests and generate coverage report (Mac)"
        },
        {
            "name": "clean",
            "command": "git restore . && git clean -xfd && dotnet restore && dotnet build",
            "description": "Restore, clean untracked files, and rebuild the project"
        },
        {
            "name": "docs",
            "command": "node .\\node_modules\\doculisp\\dist\\index.js .\\docs\\_docs.dlproj"
        }
    ],
    "collaborators": []
}